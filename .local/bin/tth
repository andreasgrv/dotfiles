#!/bin/bash

echo "tthing $1 ..."
tokens=(${1//@/ })
uname=${tokens[0]}
hostname=${tokens[1]}
if [[ "$uname" =~ "agr" ]]
then
	ssh -tt "$hostname" nice -n 19 bash -ic 'tmux new'
else
	ssh "$1" -X -t bash -ic 'tmux new'
fi

#!/bin/bash

echo "tthing $1 ..."
tokens=(${1//@/ })
uname=${tokens[0]}
hostname=${tokens[1]}
if [[ "$uname" =~ "s"[0-9]{7} ]]
then
	ssh -tt uni ssh -tt "$hostname" nice -n 19 bash -ic .bin/run_env
else
	ssh "$1" -X -t bash -ic 'tmux new'
fi
